source 'https://rubygems.org'

# Specify your gem's dependencies in jscrollpane-rails.gemspec
gemspec

group :test do
  # jquery-rails is used by the dummy application
  gem 'jquery-rails'
  gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
end